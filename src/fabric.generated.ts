// Placeholder — run `npm run fabric:generate` after configuring fabric.yaml.
// Overwritten locally; safe to commit with YOUR-* placeholders.

export const fabricConfig = {
  semanticModels: {
    migrationPulse: {
      workspaceId: 'YOUR-WORKSPACE-GUID',
      itemId: 'YOUR-SEMANTIC-MODEL-GUID',
    },
  },
} as const;
